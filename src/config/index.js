'use strict';

module.exports = {
    cassandra: {
        contactPoints: [
            '127.0.0.1'
        ],
        keyspace: 'blockchain'
    },

    auth: {
        secret: 'Authentication Token Secret Key'
    }
};
